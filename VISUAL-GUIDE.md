# EduMatch System - Complete Visual Guide

## 🏗️ Full System Architecture

```
                                    INTERNET
                                       │
                         ┌─────────────┴─────────────┐
                         │                           │
                    Browser/App                  API Client
                         │                           │
                         └─────────────┬─────────────┘
                                       │
                                   HTTP :80
                                       │
                         ┌─────────────▼─────────────┐
                         │   NGINX API GATEWAY       │
                         │   - Rate Limiting         │
                         │   - Load Balancing        │
                         │   - CORS & Security       │
                         │   - WebSocket Proxy       │
                         └───┬───┬───┬───┬───────┬───┘
                             │   │   │   │       │
                ┌────────────┘   │   │   │       └────────────┐
                │                │   │   │                    │
                │                │   │   │                    │
┌───────────────▼────┐   ┌───────▼───▼───▼───┐   ┌───────────▼──────────┐
│                    │   │                    │   │                      │
│  FRONTEND LAYER    │   │  BACKEND LAYER     │   │  INFRASTRUCTURE      │
│                    │   │                    │   │                      │
│  ┌──────────────┐  │   │  ┌──────────────┐  │   │  ┌────────────────┐  │
│  │ React.js     │  │   │  │ User Service │  │   │  │ PostgreSQL     │  │
│  │ Port: 3000   │  │   │  │ Java/Spring  │  │   │  │ - java_db      │  │
│  │              │  │   │  │ Port: 8080   │──┼───┼─►│ - matching_db  │  │
│  │ - Dashboard  │  │   │  └──────────────┘  │   │  └────────────────┘  │
│  │ - Profiles   │  │   │                    │   │                      │
│  │ - Chat UI    │  │   │  ┌──────────────┐  │   │  ┌────────────────┐  │
│  │ - Search     │  │   │  │ Scholarship  │  │   │  │ RabbitMQ       │  │
│  └──────────────┘  │   │  │ Service      │  │   │  │ Port: 5672     │  │
│                    │   │  │ Java/Spring  │  │   │  │ UI: 15672      │  │
└────────────────────┘   │  │ Port: 8080   │◄─┼───┼──┤                │  │
                         │  └──────────────┘  │   │  │ - Events       │  │
                         │                    │   │  │ - Messages     │  │
                         │  ┌──────────────┐  │   │  └────────────────┘  │
                         │  │ Chat Service │  │   │                      │
                         │  │ Java/Spring  │  │   │  ┌────────────────┐  │
                         │  │ Port: 8080   │  │   │  │ Celery Workers │  │
                         │  │ + WebSocket  │  │   │  │                │  │
                         │  └──────────────┘  │   │  │ - Profile      │  │
                         │                    │   │  │ - Scholarship  │  │
                         │  ┌──────────────┐  │   │  │ - Matching     │  │
                         │  │ Matching Svc │  │   │  └────────────────┘  │
                         │  │ Python/API   │  │   │                      │
                         │  │ Port: 8000   │  │   │  ┌────────────────┐  │
                         │  │              │  │   │  │ RabbitMQ       │  │
                         │  │ - ML Algo    │  │   │  │ Consumer       │  │
                         │  │ - TF-IDF     │  │   │  └────────────────┘  │
                         │  │ - Scoring    │  │   │                      │
                         │  └──────────────┘  │   └──────────────────────┘
                         │                    │
                         └────────────────────┘
```

## 🔄 Request Flow Examples

### Example 1: User Login
```
┌────────┐
│ Client │
└───┬────┘
    │ POST /api/auth/login
    │ {"email":"user@example.com","password":"xxx"}
    ▼
┌────────────────┐
│ Nginx Gateway  │ ─────► Check rate limit (5 req/min)
└───┬────────────┘        ✓ Allowed
    │ Proxy to user-service:8080
    ▼
┌────────────────┐
│ User Service   │ ─────► Validate credentials
└───┬────────────┘        ✓ Valid
    │ Query database
    ▼
┌────────────────┐
│ PostgreSQL     │ ─────► Find user
└───┬────────────┘        ✓ Found
    │ Return user data
    ▼
┌────────────────┐
│ User Service   │ ─────► Generate JWT token
└───┬────────────┘        ✓ Token created
    │ Return token
    ▼
┌────────────────┐
│ Nginx Gateway  │ ─────► Forward response
└───┬────────────┘
    │ 200 OK
    │ {"token":"eyJhbGc...","user":{...}}
    ▼
┌────────┐
│ Client │ ─────► Store token, redirect to dashboard
└────────┘
```

### Example 2: Get Match Score (ML)
```
┌────────┐
│ Client │
└───┬────┘
    │ POST /api/v1/match/score
    │ Authorization: Bearer eyJhbGc...
    │ {"applicant_id":1,"opportunity_id":1}
    ▼
┌────────────────┐
│ Nginx Gateway  │ ─────► Check rate limit (20 req/min)
└───┬────────────┘        ✓ Allowed
    │ Proxy to matching-service:8000
    │ Timeout: 60 seconds (ML operations)
    ▼
┌────────────────┐
│ Matching Svc   │ ─────► Validate JWT
└───┬────────────┘        ✓ Valid
    │ Query features
    ▼
┌────────────────┐
│ PostgreSQL     │ ─────► Get applicant features
│ (matching_db)  │        Get opportunity features
└───┬────────────┘        ✓ Found
    │ Return features
    ▼
┌────────────────┐
│ Matching Svc   │ ─────► Run ML algorithm
└───┬────────────┘        - TF-IDF on skills
    │                     - Cosine similarity
    │                     - GPA weighting
    │                     Takes 2-5 seconds
    ▼
┌────────────────┐
│ Matching Svc   │ ─────► Calculate overall score
└───┬────────────┘        ✓ Score: 85.5%
    │ Save to database
    ▼
┌────────────────┐
│ PostgreSQL     │ ─────► Insert matching_score
└───┬────────────┘        ✓ Saved
    │ Return response
    ▼
┌────────────────┐
│ Nginx Gateway  │ ─────► Forward response
└───┬────────────┘
    │ 200 OK
    │ {"overall_score":85.5,"details":{...}}
    ▼
┌────────┐
│ Client │ ─────► Display match percentage
└────────┘
```

### Example 3: Real-time Chat (WebSocket)
```
┌────────┐
│ Client │
└───┬────┘
    │ WebSocket Connection
    │ ws://localhost/api/ws/chat?token=eyJhbGc...
    ▼
┌────────────────┐
│ Nginx Gateway  │ ─────► Upgrade to WebSocket
└───┬────────────┘        Connection: Upgrade
    │                     Upgrade: websocket
    │ Proxy to chat-service:8080
    ▼
┌────────────────┐
│ Chat Service   │ ─────► Validate JWT token
└───┬────────────┘        ✓ Valid user
    │ Open WebSocket connection
    │ Timeout: 7 days
    ▼
    ═══════════════════════════════════
    ║    Bidirectional Connection     ║
    ═══════════════════════════════════
    ↕
┌────────┐          ┌────────────────┐
│ Client │ ────────►│ Chat Service   │
└────────┘          └───┬────────────┘
    ▲                   │ Save message
    │                   ▼
    │               ┌────────────────┐
    │               │ PostgreSQL     │
    │               └───┬────────────┘
    │                   │ Broadcast to other clients
    │                   ▼
    └───────────────────
```

### Example 4: Event-Driven Flow (RabbitMQ)
```
┌────────────────┐
│ User updates   │
│ their profile  │
└───┬────────────┘
    │ PUT /api/profiles/123
    ▼
┌────────────────┐
│ User Service   │ ─────► Update database
└───┬────────────┘        ✓ Updated
    │ Publish event
    ▼
┌────────────────┐
│ RabbitMQ       │ ─────► Topic: user.profile.updated
└───┬────────────┘        Routing key: user.profile.updated
    │ Route to queues
    ├──────────────┐
    │              │
    ▼              ▼
┌──────────┐  ┌──────────┐
│ Matching │  │ Other    │
│ Consumer │  │ Services │
└───┬──────┘  └──────────┘
    │ Receive event
    ▼
┌────────────────┐
│ Celery Queue   │ ─────► Task: process_user_profile_updated
└───┬────────────┘
    │ Process async
    ▼
┌────────────────┐
│ Celery Worker  │ ─────► Extract features
└───┬────────────┘        - Skills
    │                     - GPA
    │                     - Interests
    ▼
┌────────────────┐
│ Matching Svc   │ ─────► Update features in DB
└───┬────────────┘        ✓ Updated
    │ Recalculate scores
    ▼
┌────────────────┐
│ PostgreSQL     │ ─────► Update matching_scores
└───┬────────────┘        ✓ Scores updated
    │ Done
    ▼
```

## 📊 Service Communication Matrix

```
               │ User │ Match │ Scholar │ Chat │ Frontend │ RabbitMQ │ DB
───────────────┼──────┼───────┼─────────┼──────┼──────────┼──────────┼────
User Service   │  -   │   ❌   │    ❌    │  ❌   │    ✅     │    ✅     │ ✅
Match Service  │  ❌   │   -   │    ❌    │  ❌   │    ✅     │    ✅     │ ✅
Scholar Svc    │  ❌   │   ❌   │    -    │  ❌   │    ✅     │    ✅     │ ✅
Chat Service   │  ❌   │   ❌   │    ❌    │  -   │    ✅     │    ❌     │ ✅
Frontend       │  ✅   │   ✅   │    ✅    │  ✅   │    -     │    ❌     │ ❌
───────────────┴──────┴───────┴─────────┴──────┴──────────┴──────────┴────

Legend:
✅ Direct communication
❌ No direct communication (communicate via Gateway or RabbitMQ)
```

## 🔒 Security Layers

```
┌─────────────────────────────────────────────────────────────┐
│ Layer 1: Network Security                                   │
│ - Nginx rate limiting (5-100 req/min per endpoint)         │
│ - CORS policy (Access-Control-Allow-Origin)                │
│ - Request size limit (10MB)                                │
│ - Security headers (XSS, Frame Options)                     │
└───────────────────────┬─────────────────────────────────────┘
                        │
┌───────────────────────▼─────────────────────────────────────┐
│ Layer 2: Application Security                              │
│ - JWT authentication (Bearer token)                         │
│ - Role-based authorization (STUDENT, ADMIN, UNIVERSITY)     │
│ - Input validation (Pydantic, Bean Validation)             │
│ - Password hashing (BCrypt)                                 │
└───────────────────────┬─────────────────────────────────────┘
                        │
┌───────────────────────▼─────────────────────────────────────┐
│ Layer 3: Database Security                                  │
│ - Separate databases per service                            │
│ - Parameterized queries (Prisma, SQLAlchemy)               │
│ - Connection pooling                                        │
│ - Access control (user/password per service)                │
└─────────────────────────────────────────────────────────────┘
```

## 📈 Performance Characteristics

```
Service          Avg Response Time    Max Throughput    Resource Usage
────────────────────────────────────────────────────────────────────────
Gateway          < 10ms               10k+ req/s        CPU: 0.5, RAM: 50MB
User Service     50-200ms             1k req/s          CPU: 1.0, RAM: 1GB
Matching (CRUD)  100-300ms            500 req/s         CPU: 2.0, RAM: 2GB
Matching (ML)    2-5 seconds          20 req/min        CPU: 2.0, RAM: 2GB
Scholarship      50-200ms             1k req/s          CPU: 1.0, RAM: 1GB
Chat (HTTP)      50-200ms             1k req/s          CPU: 1.0, RAM: 1GB
Chat (WS)        < 50ms               10k connections   CPU: 1.0, RAM: 1GB
Frontend         Static files         CDN-backed        CPU: 0.5, RAM: 512MB
────────────────────────────────────────────────────────────────────────

Database         Query Time           Connections       Storage
────────────────────────────────────────────────────────────────────────
java_db          1-50ms               10-50 active      20GB
matching_db      1-50ms (read)        10-50 active      10GB
                 100ms-1s (ML)
────────────────────────────────────────────────────────────────────────

Message Queue    Latency              Throughput        Memory
────────────────────────────────────────────────────────────────────────
RabbitMQ         < 5ms                10k msg/s         1GB
────────────────────────────────────────────────────────────────────────
```

## 🔍 Monitoring Dashboard (Conceptual)

```
┌─────────────────────────────────────────────────────────────────┐
│ EduMatch System Dashboard                          🟢 All Healthy│
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│ Gateway Metrics                                                 │
│ ┌─────────────┬─────────────┬─────────────┬─────────────┐      │
│ │ Requests    │ Avg Latency │ Error Rate  │ Rate Limits │      │
│ │ 15,420/min  │ 45ms        │ 0.3%        │ 12 hits/min │      │
│ └─────────────┴─────────────┴─────────────┴─────────────┘      │
│                                                                 │
│ Service Health                                                  │
│ ┌─────────────┬─────────┬───────────┬────────────┐             │
│ │ Service     │ Status  │ Resp Time │ Uptime     │             │
│ ├─────────────┼─────────┼───────────┼────────────┤             │
│ │ User        │ 🟢 Up   │ 120ms     │ 99.9%      │             │
│ │ Matching    │ 🟢 Up   │ 3.2s      │ 99.5%      │             │
│ │ Scholarship │ 🟢 Up   │ 95ms      │ 99.9%      │             │
│ │ Chat        │ 🟢 Up   │ 80ms      │ 99.9%      │             │
│ │ Frontend    │ 🟢 Up   │ 15ms      │ 100%       │             │
│ └─────────────┴─────────┴───────────┴────────────┘             │
│                                                                 │
│ Database                                                        │
│ ┌─────────────┬──────────────┬────────────┬─────────────┐      │
│ │ Database    │ Connections  │ Queries/s  │ Disk Usage  │      │
│ ├─────────────┼──────────────┼────────────┼─────────────┤      │
│ │ java_db     │ 25/100       │ 450        │ 12GB/20GB   │      │
│ │ matching_db │ 18/100       │ 120        │ 5GB/10GB    │      │
│ └─────────────┴──────────────┴────────────┴─────────────┘      │
│                                                                 │
│ Message Queue                                                   │
│ ┌──────────────────┬────────────┬───────────┬──────────┐       │
│ │ Queue            │ Messages   │ Rate      │ Consumers│       │
│ ├──────────────────┼────────────┼───────────┼──────────┤       │
│ │ user.profile.*   │ 45         │ 12/min    │ 1        │       │
│ │ scholarship.*    │ 120        │ 35/min    │ 1        │       │
│ └──────────────────┴────────────┴───────────┴──────────┘       │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## 🚦 Traffic Flow Visualization

```
Time: 10:00:00 - 10:00:10 (10 seconds)

Gateway:    ████████████████████████████ 1,250 requests
            │
            ├─► User Service:      ████████████ 450 req (36%)
            │   └─► /api/auth/*:          █████ 180 (14%)
            │   └─► /api/users/*:         ███ 120 (10%)
            │   └─► /api/profiles/*:      ████ 150 (12%)
            │
            ├─► Matching Service:  ██ 50 req (4%)
            │   └─► /api/v1/match/*:      █ 20 (2%)
            │   └─► /recommendations/*:   █ 30 (2%)
            │
            ├─► Scholarship Svc:   ███████ 280 req (22%)
            │   └─► /api/scholarships/*:  ████ 150 (12%)
            │   └─► /api/applications/*:  ███ 130 (10%)
            │
            ├─► Chat Service:      █████████ 350 req (28%)
            │   └─► /api/messages/*:      ████ 140 (11%)
            │   └─► /api/conversations/*: ███ 120 (10%)
            │   └─► WebSocket:            ██ 90 (7%)
            │
            └─► Frontend:          ████ 120 req (10%)
                └─► Static assets:        ████ 120 (10%)

Response Codes:
2xx (Success):  ████████████████████████ 1,190 (95.2%)
4xx (Client):   ██ 50 (4.0%)
5xx (Server):   █ 10 (0.8%)
```

## 📦 Docker Container Network

```
┌─────────────────────────────────────────────────────────────┐
│ Docker Network: edumatch-net (bridge)                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌────────────────┐      ┌────────────────┐               │
│  │ api-gateway    │      │ frontend       │               │
│  │ nginx:alpine   │      │ node:18-alpine │               │
│  │ :80            │      │ :3000          │               │
│  └────────────────┘      └────────────────┘               │
│                                                             │
│  ┌────────────────┐      ┌────────────────┐               │
│  │ user-service   │      │ scholarship-   │               │
│  │ openjdk:17     │      │ service        │               │
│  │ :8080          │      │ :8080          │               │
│  └────────────────┘      └────────────────┘               │
│                                                             │
│  ┌────────────────┐      ┌────────────────┐               │
│  │ chat-service   │      │ matching-      │               │
│  │ openjdk:17     │      │ service        │               │
│  │ :8080          │      │ python:3.10    │               │
│  └────────────────┘      │ :8000          │               │
│                          └────────────────┘               │
│                                                             │
│  ┌────────────────┐      ┌────────────────┐               │
│  │ matching-      │      │ matching-      │               │
│  │ celery-worker  │      │ consumer       │               │
│  │ python:3.10    │      │ python:3.10    │               │
│  └────────────────┘      └────────────────┘               │
│                                                             │
│  ┌────────────────┐      ┌────────────────┐               │
│  │ java-db        │      │ matching-db    │               │
│  │ postgres:14    │      │ postgres:14    │               │
│  │ :5433          │      │ :5432          │               │
│  └────────────────┘      └────────────────┘               │
│                                                             │
│  ┌────────────────┐                                        │
│  │ rabbitmq       │                                        │
│  │ rabbitmq:3.9   │                                        │
│  │ :5672, :15672  │                                        │
│  └────────────────┘                                        │
│                                                             │
│ Total: 12 containers                                        │
└─────────────────────────────────────────────────────────────┘

External Ports Exposed:
- 80      → api-gateway (main entry)
- 3000    → frontend (direct access)
- 5432    → matching-db (direct access)
- 5433    → java-db (direct access)
- 5672    → rabbitmq (AMQP)
- 15672   → rabbitmq (management UI)
- 8000    → matching-service (direct access)
- 8081    → user-service (direct access)
- 8082    → scholarship-service (direct access)
- 8083    → chat-service (direct access)
```

## 🎯 Use Case Scenarios

### Scenario 1: Student Searches Scholarships
```
1. Student opens app → Frontend loads
2. Student types search → /api/scholarships?q=computer+science
3. Gateway routes → Scholarship Service
4. Service queries DB → Returns 15 results
5. Student clicks scholarship → /api/scholarships/123
6. Gateway routes → Scholarship Service
7. Service gets details → Returns full info
8. Student clicks "Apply" → /api/applications (POST)
9. Gateway checks auth → Valid JWT
10. Service creates application → Publishes event to RabbitMQ
11. Matching Consumer receives event → Triggers Celery task
12. Celery Worker calculates match score → Updates matching_db
13. Student gets confirmation → Application submitted
```

### Scenario 2: University Posts New Scholarship
```
1. University admin logs in → JWT token received
2. Admin creates scholarship → /api/scholarships (POST)
3. Gateway checks auth → Valid JWT, role=ADMIN
4. Scholarship Service creates → Inserts into DB
5. Service publishes event → RabbitMQ: scholarship.created
6. Matching Consumer receives → Parses event
7. Celery task triggered → process_scholarship_created
8. Worker extracts features → Skills, GPA, field of study
9. Worker calculates scores → For all eligible students
10. Students get notifications → "New scholarship matches you!"
11. Background recommendations → Updated in matching_db
```

### Scenario 3: Students Chat in Real-time
```
1. Student A opens chat → WebSocket connection to gateway
2. Gateway upgrades connection → Proxy to Chat Service
3. Student A sends message → WS message to server
4. Chat Service receives → Saves to database
5. Service identifies recipients → Student B is online
6. Service broadcasts → WebSocket to Student B
7. Student B receives → Message displayed instantly
8. Student B replies → Same flow in reverse
9. Connection persists → Up to 7 days or until closed
10. Offline messages → Stored and delivered on next login
```

---

**Legend:**
- 🟢 Service healthy
- 🟡 Service degraded
- 🔴 Service down
- ✅ Communication allowed
- ❌ No direct communication
- ► Flow direction
- ═ Bidirectional connection
- █ Visual representation of metrics
